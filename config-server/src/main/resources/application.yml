server:
  port: 9999

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/alongsoCJR/SpringCloud-Config-Center.git
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7900/eureka/

